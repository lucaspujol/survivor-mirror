POSTGRES_USER=chomagego
POSTGRES_PASSWORD=chomagego
POSTGRES_DB=chomagego
POSTGRES_HOST_PORT=5433

# Signs the auth JWTs. Generate your own:
#   python3 -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=
JWT_EXPIRE_DAYS=7
# Set to true once served over https.
COOKIE_SECURE=false
